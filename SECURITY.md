# Security

repobeacon v1 does not make network calls and does not require secrets.

If future providers add GitHub API access, they must be opt-in, documented, and disabled by default. Never place tokens in fixture files or generated dashboards.

Report security issues privately to the maintainer rather than opening a public issue.
